services:
  - type: web
    name: auto-scrolling-saas
    env: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 10000
      - key: DEBUG
        value: "False"
    disk:
      name: auto-scrolling-storage
      mountPath: /app/backend/storage
      sizeGB: 1
